[project]
name = "bracketbot-baymax"
version = "0.1.0"
description = "Safety-first interaction, gestures, and assistant tooling for BracketBot"
readme = "README.md"
requires-python = ">=3.10"
dependencies = []

[project.optional-dependencies]
vision = [
    "emotiefflib==1.1.1",
    "ultralytics>=8.3",
]
dev = [
    "pytest>=8",
]

[tool.uv]
package = false
